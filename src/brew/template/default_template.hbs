# typed: false
# frozen_string_literal: true

# DO NOT EDIT THIS FILE

class {{ name }} < Formula
  {{ #if description }}
  desc "{{ description }}"
  {{ /if }}
  {{ #if homepage }}
  homepage "{{ homepage }}"
  {{ /if }}
  url "{{ url }}"
  version "{{ version }}"
  sha256 "{{ tarball_checksum }}"
  {{ #if license }}
  license "{{ license }}"
  {{ /if }}
  {{ #if head }}
  head "{{ head.url }}"{{ #if head.branch }}, branch: "{{ head.branch }}"{{ /if }}
  {{ /if }}

  def install
    {{{ ../../install_info }}}
  end
  {{ #if caveats}}

  def caveats
    "{{ caveats }}"
  end
  {{ /if }}
  {{ #if test }}

  test do
    {{{ test }}}
  end
  {{ /if }}
end
